export default defineAppConfig({
  ui: {
    colors: {
      primary: 'cyan',
      neutral: 'neutral',
    },

    button: {
      defaultVariants: {
        color: 'neutral',
      },
    },
  },
});
